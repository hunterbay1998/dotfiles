-- git.yazi: show git status signs next to files
require("git"):setup {
	-- Order of status signs showing in the linemode
	order = 1500,
}

-- full-border.yazi: draw a border around all three panes
require("full-border"):setup()
